from setuptools import setup, find_packages

setup(
    name='kinetics_gs',
    description='A...',
    author='Gal Sapir',
    packages=find_packages(),
    install_requires=['logbook', 'pandas', 'numpy', 'matplotlib'],
    url='github...',
    # entry_points={
    #     'console_scripts': ['kinetics-gs=kinetics:main'],
    # }
)
